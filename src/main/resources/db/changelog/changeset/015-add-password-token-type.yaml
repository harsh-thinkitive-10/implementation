databaseChangeLog:
  - changeSet:
      id: 007-add-password-token-type
      author: harsh
      changes:

        - addColumn:
            tableName: password_reset_token
            columns:
              - column:
                  name: token_type
                  type: VARCHAR(30)
                  defaultValue: PASSWORD_RESET
                  constraints:
                    nullable: false